node {
    stage('Checkout') {
        checkout scm
    }
    stage('Test') {
        echo "tested"
    }
    stage('Build') {
        echo "built"
    }
    stage('deploy') {
        echo "Deployed"
    }
}
